workflow_dispatch:
workflow_call:
pull_request:
push:
  branches:
    {{#each branches}}
    - {{ . }}
    {{/each}}
schedule:
  # "At 09:00 UTC (02:00 PT) on Monday" https://crontab.guru/#0_9_*_*_1
  - cron: "0 9 * * 1"
