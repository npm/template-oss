- name: Lint
  run: {{ rootNpmPath }} run lint --ignore-scripts $\{{ steps.changed-workspaces.outputs.flags }} {{~#if jobRunFlags}} {{ jobRunFlags }}{{/if}}
- name: Post Lint
  run: {{ rootNpmPath }} run postlint --ignore-scripts $\{{ steps.changed-workspaces.outputs.flags }} {{~#if jobRunFlags}} {{ jobRunFlags }}{{/if}}
